/node_modules

# Local secrets
.env

# SQLite database files
/data
*.sqlite
*.sqlite3

# OS / editor cruft
.DS_Store
npm-debug.log*

# Vercel CLI link metadata
.vercel
.env*

# Tooling for whoever is editing the repo, rather than anything the app runs.
# .agents/ and skills-lock.json arrived with the Neon marketplace integration.
.claude
.agents
skills-lock.json
